pipeline {
    agent any
    stages {
        stage("build") {
            steps {
                echo "build process started"
            }
        }
        stage("test") {
            steps {
                echo "test process started"
            }
        }
        stage("deploy") {
            steps {
                echo "deploy started"
            }
        }
    }
}
